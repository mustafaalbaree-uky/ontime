import Foundation

/// Compiled into both the app and the widget extension. Empty for now —
/// later stages put the Live Activity attributes and App Group constants
/// here once both targets need to agree on them.
enum OnTimeShared {}
